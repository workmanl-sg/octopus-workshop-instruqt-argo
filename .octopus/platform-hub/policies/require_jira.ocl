name = "Require Jira"
violation_action = "block"

conditions {
    rego = <<-EOT
            package require_jira
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package require_jira
            
            default evaluate := true
            EOT
}